name: ai_powered_tourists_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  flutter_screenutil: ^5.9.3
  get: ^4.7.2
  get_storage: ^2.1.1
  google_fonts: ^6.3.2
  google_maps_flutter: ^2.13.1
  geocoding: ^3.0.0
  geolocator: ^13.0.1
  http: ^1.2.2
  image_picker: ^1.2.0
  permission_handler: ^12.0.1
  pinput: ^5.0.2
  just_audio: ^0.9.42

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/sounds/
